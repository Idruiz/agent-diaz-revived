name: Apply activity semantic boundary fix

on:
  push:
    branches:
      - codex/activity-capacity-hotfix
    paths:
      - scripts/apply-activity-boundary-fix.mjs
      - .github/workflows/apply-activity-boundary-fix.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/activity-capacity-hotfix
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply source patch
        run: node scripts/apply-activity-boundary-fix.mjs
      - name: Commit source changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src
          git diff --cached --quiet && exit 0
          git commit -m "Validate semantic activities before physical compilation"
          git push origin HEAD:codex/activity-capacity-hotfix
