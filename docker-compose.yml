services:
  agent-diaz:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - diaz-data:/app/data
      - diaz-artifacts:/app/artifacts
      - diaz-uploads:/app/uploads

volumes:
  diaz-data:
  diaz-artifacts:
  diaz-uploads:
